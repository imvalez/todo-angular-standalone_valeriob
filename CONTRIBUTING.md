# Contributing

Thanks for contributing to Todo Angular Standalone.

## Workflow

1. Open an issue for non-trivial changes.
2. Branch from `main`.
3. Keep pull requests focused.
4. Update tests and docs together with code changes.

## Local checks

```bash
cd valerio-bottari
npm ci
npm run build
npm run test:ci
```

Maintainer: @valbot
